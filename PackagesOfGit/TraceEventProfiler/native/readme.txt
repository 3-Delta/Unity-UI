Bee is a cross-platform build tool developed internally at Unity. This package uses it to build the native DLLs for the package.

The following command will build DLLs for Android and OSX:
mono bee.exe android osx

Or on Windows:
bee.exe windows android
